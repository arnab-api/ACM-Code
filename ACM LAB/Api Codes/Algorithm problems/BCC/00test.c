#include<bits/stdc++.h>
using namespace std;
#define M 1111

int arr[M];

int main()
{
    int n,i,j,q,k;
    while(scanf("%d",&n)==1){
        for(i=0;i<n;i++) scanf("%d",&arr[i]);
        sort(arr,arr+n);
        scanf("%d",&q);
        while(q--){
            scanf("%d",&k);
            int idx = lower_bound()
        }
    }
    return 0;
}
